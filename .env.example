# =============================================================================
# CSE363 Phase 2 - Environment Configuration
# =============================================================================
# IMPORTANT: Copy this file to .env and set ALL required values before running.
#
# Variables marked (REQUIRED) MUST be set - docker-compose will fail without them.
# Variables marked (optional) have reasonable defaults.
# =============================================================================

# =============================================================================
# AWS CREDENTIALS (REQUIRED)
# =============================================================================
# Get these from AWS Academy Learner Lab - they expire every 4 hours!
# Go to: Learner Lab > AWS Details > Show (next to AWS CLI)

AWS_ACCESS_KEY_ID=        # (REQUIRED) Your AWS access key
AWS_SECRET_ACCESS_KEY=    # (REQUIRED) Your AWS secret key
AWS_SESSION_TOKEN=        # (REQUIRED) Your AWS session token
AWS_REGION=us-east-1      # (optional) Default region

# =============================================================================
# S3 BUCKET NAMES (optional - will use defaults)
# =============================================================================
S3_BUCKET_NAME=learner-lab-audio

# =============================================================================
# SECURITY CREDENTIALS (REQUIRED)
# =============================================================================
# IMPORTANT: Generate strong, unique values for production use!
# Example: openssl rand -base64 32

JWT_SECRET=               # (REQUIRED) Secret key for JWT tokens - CHANGE THIS!
JWT_EXPIRATION_HOURS=24   # (optional) Token expiration time

# =============================================================================
# DATABASE CREDENTIALS (REQUIRED)
# =============================================================================
# IMPORTANT: Use strong passwords in production!
# Example: openssl rand -base64 16

# Chat Database
CHAT_DB_USER=             # (REQUIRED) Chat database username
CHAT_DB_PASSWORD=         # (REQUIRED) Chat database password

# Document Database  
DOCUMENT_DB_USER=         # (REQUIRED) Document database username
DOCUMENT_DB_PASSWORD=     # (REQUIRED) Document database password

# Quiz Database
QUIZ_DB_USER=             # (REQUIRED) Quiz database username
QUIZ_DB_PASSWORD=         # (REQUIRED) Quiz database password

# =============================================================================
# SERVICE VERSIONS (optional - for image tagging)
# =============================================================================
GATEWAY_VERSION=1.0.0
TTS_VERSION=1.0.0
STT_VERSION=1.0.0
CHAT_VERSION=1.0.0
DOCUMENT_VERSION=1.0.0
QUIZ_VERSION=1.0.0

# =============================================================================
# CONTAINER REGISTRY (optional)
# =============================================================================
# Uncomment and set for ECR deployment
# ECR_REGISTRY=123456789012.dkr.ecr.us-east-1.amazonaws.com

# =============================================================================
# APPLICATION CONFIG (optional - have sensible defaults)
# =============================================================================
LOG_LEVEL=INFO
RATE_LIMIT_REQUESTS=100
RATE_LIMIT_WINDOW=60
REQUEST_TIMEOUT=30
MAX_FILE_SIZE=10485760

CHAT_DB_PASSWORD=pass
DOCUMENT_DB_PASSWORD=pass
QUIZ_DB_PASSWORD=Mpass

